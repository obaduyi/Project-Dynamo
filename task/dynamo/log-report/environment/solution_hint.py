# Solution hint - shows the expected structure
# Do not use this in your solution; this is only for reference.

import json

# Example of the expected output structure:
example_report = {
    "total_requests": 0,
    "unique_ips": 0,
    "top_path": "/"
}

print(json.dumps(example_report, indent=2))
